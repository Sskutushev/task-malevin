{
  "name": "task-malevin",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "format": "npm --prefix backend run format && npm --prefix frontend run format",
    "typecheck": "npm --prefix backend run typecheck && npm --prefix frontend run typecheck",
    "eslint": "npm --prefix backend run eslint && npm --prefix frontend run eslint",
    "test": "npm --prefix backend run test && npm --prefix frontend run test",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "ci:check": "npm run format && npm run typecheck && npm run eslint && npm run test && npm run build"
  }
}
